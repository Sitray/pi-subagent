import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function subagentStatusExtension(_pi: ExtensionAPI): void {
	// Lifecycle and UI registrations are introduced in their owning work units.
}
